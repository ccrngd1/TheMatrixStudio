# Git
.git
.gitignore

# Python
__pycache__
*.py[cod]
*$py.class
*.so
.Python
*.egg-info
dist/
build/
*.egg
.eggs/
.venv/
venv/
ENV/
env/

# Testing
.pytest_cache
.coverage
htmlcov/

# IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Data
data/
*.db
*.db-shm
*.db-wal

# Environment
.env
.env.local

# Documentation
*.md
!README.md

# MasterControl
.mastercontrol/

# Node — never copy host node_modules or a stale local build into the image;
# the frontend-builder stage installs deps and builds fresh.
node_modules/
**/node_modules/
frontend/dist/
frontend/build/
matrix_studio/static/

# OS
.DS_Store
Thumbs.db
# The CDK app: aws-cdk-lib is ~60 MB and has no business in a runtime image.
infra/
.venv/

# Unrelated vendored material sitting in the repo root. Git-ignored but NOT
# docker-ignored until now, so every build was copying 125 MB into the context
# for nothing — which is also 125 MB uploaded on every CDK asset change.
8bit-agents-activation-demo-main/
